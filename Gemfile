source 'https://rubygems.org'

# git_source(:github) do |repo_name|
#   repo_name = "#{repo_name}/#{repo_name}" unless repo_name.include?('/')
#   "https://github.com/#{repo_name}.git"
# end

gem 'scoruby'
gem 'ai4r'
gem 'classifier-reborn'
gem 'cerebrum'
# bundle config build.ruby-opencv --with-opencv-dir=/Users/max/anaconda3/lib/python3.6/site-packages/
gem 'ruby-opencv', github: 'ruby-opencv', branch: 'experimental/opencv3'
# Install Tensorflow first https://www.tensorflow.org/install/install_mac
# gem 'tensorflow'
